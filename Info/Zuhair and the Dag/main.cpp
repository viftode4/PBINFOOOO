#include <bits/stdc++.h>
using namespace std;
int n, m;
int x, y;
int main()
{
    cin >> n >> m;

    for( int i = 1; i <= m; i++ )
        {
            fscanf
            cout << ( x > y ) ? 1 : 0;
        }

    cout << endl;
    return 0;
}
