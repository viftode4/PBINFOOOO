#include <bits/stdc++.h>
#define st first
#define nd second
#define pb push_back
#define mp make_pair
#define ll long long
using namespace std;
ifstream fin( "xxx.in" );
ofstream fout( "xxx.in" );
void fast()
{
    cin.tie( 0 );
    ios_base::sync_with_stdio( 0 );
}
int di[] = {0, 1, 0, -1};
int dj[] = {1, 0, -1, 0};
char d[] = {'R', 'U', 'L', 'D'};
char sol[1005][1005];
int a[1005][1005];
queue<pair<int, int>>q;
int main()
{
    fast();
    cin >> n;

    for( int i = 1; i <= n; i++ )
        for( int j = 1; j <= n; j++ )
            {
                int x, y;
                cin >> x >> y;
                if(x==-1
            }

    return 0;
}
