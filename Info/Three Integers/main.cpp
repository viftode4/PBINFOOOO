#include <bits/stdc++.h>
#define st first
#define nd second
#define pb push_back
#define mp make_pair
#define ll long long
using namespace std;
ifstream fin( "xxx.in" );
ofstream fout( "xxx.in" );
void fast()
{
    cin.tie( 0 );
    ios_base::sync_with_stdio( 0 );
}
int t;
int a, b, c;
vector< pair< pair<int, int>, int> > sol[3];
int main()
{
    fast();
    cin >> t;

    while( t-- )
        {
            cin >> a >> b >> c;
            int sol[4] = {0, 0, 0, 0};

        }

    return 0;
}
