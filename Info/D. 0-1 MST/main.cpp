#include <bits/stdc++.h>
using namespace std;
int n, m;
vector<int>g[100005];
vector<int>g[100005];
int main()
{
    cin >> n;

    for( int i = 1; i <= m; i++ )
        {
            int x, y;
            fr[x]
            cin >> x >> y;
            g[x].push_back( y );
            g[y].push_back( x );
            fr[x]++;
            fr[y]++;
        }

    return 0;
}
