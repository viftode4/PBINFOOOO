#include <bits/stdc++.h>
using namespace std;
ifstream fin( "puteri4.in" );
ofstream fout( "puteri4.out" );
int cer, a, b;
struct sol
{
    int x, y;
} sol[200];
int main()
{

    cin >> cer;

    for( int i = 1; i <= n; i++ )
        {
            cin >> a >> b;
            sol
        }

    return 0;
}
