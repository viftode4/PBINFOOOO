#include <bits/stdc++.h>
using namespace std;

ifstream fin ( "unu.in" );
ofstream fout ( "unu.out" );

int n, i, x, y, z;
int max1, max2;
int main()
{
    while ( fin >> x >> y >> z )
        cout << x<<' ';

    return 0;
}
